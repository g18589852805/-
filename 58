<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=yes">
    <title>PC28 · 加拿大28实时开奖系统</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            background: linear-gradient(135deg, #0f0c29, #302b63, #24243e);
            font-family: system-ui, -apple-system, 'Segoe UI', sans-serif;
            padding: 16px;
            min-height: 100vh;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
        }
        .card {
            background: rgba(255,255,255,0.1);
            backdrop-filter: blur(12px);
            border-radius: 32px;
            padding: 20px;
            margin-bottom: 20px;
            border: 1px solid rgba(255,215,0,0.3);
            transition: all 0.2s;
        }
        .card-title {
            font-size: 1.2rem;
            font-weight: bold;
            color: #ffd700;
            margin-bottom: 16px;
            border-left: 4px solid #ffd700;
            padding-left: 12px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .live-badge {
            background: #e74c3c;
            color: white;
            font-size: 0.7rem;
            padding: 4px 12px;
            border-radius: 20px;
            animation: blink 1s infinite;
        }
        @keyframes blink {
            0% { opacity: 1; }
            50% { opacity: 0.5; }
            100% { opacity: 1; }
        }
        .current-number {
            font-size: 4.5rem;
            font-weight: bold;
            color: #ffd700;
            text-align: center;
            text-shadow: 0 0 20px #ffd700;
            background: rgba(0,0,0,0.3);
            width: 140px;
            height: 140px;
            line-height: 140px;
            border-radius: 70px;
            margin: 10px auto;
        }
        .combination-badge {
            display: inline-block;
            padding: 6px 24px;
            border-radius: 40px;
            font-weight: bold;
            margin-top: 8px;
        }
        .big { background: #e74c3c; color: white; }
        .small { background: #2ecc71; color: white; }
        .single { background: #3498db; color: white; }
        .double { background: #9b59b6; color: white; }
        .btn {
            background: linear-gradient(95deg, #ffd700, #ff8c00);
            border: none;
            width: 100%;
            padding: 12px;
            border-radius: 50px;
            font-weight: bold;
            font-size: 1rem;
            cursor: pointer;
            margin-top: 12px;
            transition: transform 0.1s;
        }
        .btn:active { transform: scale(0.97); }
        .btn-orange {
            background: linear-gradient(95deg, #e67e22, #d35400);
            color: white;
        }
        .history-item {
            background: rgba(255,255,255,0.08);
            border-radius: 16px;
            padding: 10px 12px;
            margin-bottom: 8px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 8px;
        }
        .history-new {
            background: rgba(255,215,0,0.2);
            border-left: 3px solid #ffd700;
        }
        .history-period { color: #ffd700; font-weight: bold; }
        .history-number { font-size: 1.2rem; font-weight: bold; color: white; }
        .history-combo { padding: 2px 12px; border-radius: 20px; font-size: 0.7rem; font-weight: bold; }
        .history-date { color: #aaa; font-size: 0.7rem; }
        .stats-grid {
            display: flex;
            gap: 12px;
            margin-bottom: 16px;
            flex-wrap: wrap;
        }
        .stat-card {
            background: rgba(0,0,0,0.3);
            border-radius: 20px;
            padding: 12px;
            flex: 1;
            text-align: center;
            min-width: 100px;
        }
        .stat-label { font-size: 0.7rem; color: #aaa; }
        .stat-value { font-size: 1.3rem; font-weight: bold; color: #ffd700; }
        .pred-grid {
            display: flex;
            gap: 12px;
            flex-wrap: wrap;
            justify-content: center;
        }
        .pred-item {
            background: rgba(0,0,0,0.4);
            border-radius: 40px;
            padding: 8px 20px;
            text-align: center;
        }
        .pred-label { font-size: 0.7rem; color: #aaa; }
        .pred-value { font-size: 1.1rem; font-weight: bold; color: #ffd700; }
        .status { text-align: center; color: #aaa; font-size: 0.7rem; margin-top: 10px; }
        .info-note { font-size: 0.65rem; color: #888; text-align: center; margin-top: 10px; }
        .footer { text-align: center; font-size: 0.6rem; color: #666; margin-top: 16px; }
        .blur { filter: blur(5px); opacity: 0.6; poi
